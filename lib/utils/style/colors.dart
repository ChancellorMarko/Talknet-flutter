import 'package:flutter/material.dart';

/// Guia de Cores do App
/// Este arquivo contém toda a configuração de tema, cores e componentes
/// otimizados para usuários do aplicativo.

// ==================== PALETA DE CORES ====================

class AppColors {
  /// Cor azul primária do aplicativo
  static const Color primaryBlue = Color(0xFF2E5BFF);
  /// Cor verde primária do aplicativo
  static const Color primaryGreen = Color(0xFF00A896);

  /// Cor amarela secundária do aplicativo
  static const Color secondaryGold = Color(0xFFFFD166);
  /// Cor coral secundária do aplicativo
  static const Color secondaryCoral = Color(0xFFFF8E6E);

  /// Cor de fundo branco
  static const Color backgroundWhite = Color(0xFFFFFFFF);
  /// Cor de fundo claro do aplicativo
  static const Color backgroundLight = Color(0xFFF8F9FA);
  /// Cor de fundo escuro do aplicativo
  static const Color backgroundCard = Color(0xFFFFFFFF);

  /// Cor de texto escuro
  static const Color textDark = Color(0xFF2D3748);
  /// Cor de texto médio
  static const Color textMedium = Color(0xFF4A5568);
  /// Cor de texto claro
  static const Color textLight = Color(0xFF718096);
  /// Cor de texto branco
  static const Color textWhite = Color(0xFFFFFFFF);

  /// Cor de status sucesso
  static const Color success = Color(0xFF48BB78);
  /// Cor de status aviso
  static const Color warning = Color(0xFFFBD38D);
  /// Cor de status erro
  static const Color error = Color(0xFFF56565);
  /// Cor de informação
  static const Color info = Color(0xFF4299E1);

  /// Cor de sombra leve
  static const Color shadowLight = Color(0x0F000000);
  /// Cor de sombra média
  static const Color shadowMedium = Color(0x1A000000);
}
