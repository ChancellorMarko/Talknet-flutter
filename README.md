# flutter_talknet_app

- Para rodar esse projeto você irá precisar criar uma cópia do arquivo `.env example` e preencher os campos com suas informações da base de dados supabase como no exemplo:

```
SUPABASE_URL=https://super.supabaseurl.here
SUPABASE_KEY=superSecretKeyHere
```